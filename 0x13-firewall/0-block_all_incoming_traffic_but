sudo apt-get update
sudo apt-get install -y ufw

# Allow Outgoing traffic but Deny incoming traffic
sudo ufw default deny incoming
sudo ufw default allow outgoing

# Ports allowed
sudo ufw allow 22
sudo ufw allow 80
sudo ufw allow 443

# Enable Fire Wall
sudo ufw enable
